using System;
using System.Linq;

namespace QLVT_DATHANG.Utility
{
   public static class ExtentionMethod
   {
      //public static void ForEach<TElement>(this IEnumerable<TElement> source, Func<TElement, TElement> func)
      //{
      //   //var list = source.ToList();
      //   //list.ForEach()
      //   //for (int i = 0; i < source.Count(); i++)
      //   //{
      //   //   list[i] = func(list[i]);
      //   //}
      //}
   }
}